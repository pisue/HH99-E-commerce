spring:
  application:
    name: e-commerce
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format-sql: true
        use-sql-comments: true
        generate-statistics: true

logging:
  level:
    org.hibernate:
      type:
        descriptor.sql: trace